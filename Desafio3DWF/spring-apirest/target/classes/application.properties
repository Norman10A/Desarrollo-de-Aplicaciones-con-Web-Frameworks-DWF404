# ====== App ======
spring.application.name=spring-apirest
server.port=8080

# ====== MySQL ======
# OJO: Cambia "desafio" por el nombre real de tu BD si usaste otro.
spring.datasource.url=jdbc:mysql://localhost:3306/desafio?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
spring.datasource.username=desafio_practico
spring.datasource.password=4321

# ====== JPA / Hibernate ======
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
# (Opcional, recomendado)
spring.jpa.open-in-view=false

# ====== JWT ======
# Cambia este secreto por uno largo y dif�cil en producci�n
jwt.secret=MiSecretoMuyLargoYSeguroQueCambiaEnProduccion12345
# 1 hora en ms
jwt.expirationMs=3600000

# ====== Seguridad Web (evitar login form HTML y forzar 401 en API) ======
# Si tienes un AuthenticationEntryPoint/handler ya est� bien; estas l�neas son por si
# usas propiedades para ajustar (tu config Java manda sobre esto).

# ====== OAuth2 GitHub ======
spring.security.oauth2.client.registration.github.client-id=Ov23lil8Kq4bZ6bETNlr
spring.security.oauth2.client.registration.github.client-secret=250ec858ac8b501188758d2fbe940314cd28b663
spring.security.oauth2.client.registration.github.scope=user:email
spring.security.oauth2.client.registration.github.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# === Modo de respuesta tras login con GitHub ===
# Opci�n A (FRONT): redirige a tu front agregando ?token=<JWT>
# app.oauth2.success-redirect-url=http://localhost:3000/login/success

# Opci�n B (SIN FRONT / Postman): deja la l�nea de arriba COMENTADA.
# Entonces tu SuccessHandler responder� JSON con {"token":"<JWT>"}.

# ====== Logging �til (opcional) ======
logging.level.org.springframework.security=INFO
logging.level.sv.edu.udb=INFO


